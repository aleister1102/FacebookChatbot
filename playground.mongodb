const database = 'chatbot';

// The current database to use.
use(database);

// db.users.insertOne({_id: 1, memes: 5})

// decrement after viewing a meme
db.users.updateOne({_id: 1}, {$inc: {memes: -1}})

// reset counter after one day
db.users.updateOne({_id: 1}, {$set: {memes: 5}})

db.users.find({})
